# Clerk — Dashboard → API keys (https://dashboard.clerk.com)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Vercel: Project → Settings → Environment Variables — add BOTH keys for Production
# (and Preview if you use it). Redeploy after changing env vars.

# Optional: match your deployed URLs (defaults work for /sign-in and /sign-up)
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
